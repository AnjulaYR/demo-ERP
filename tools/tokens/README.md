# Token Tooling

The current token files are committed as JSON and CSS. A future step can add Style Dictionary or a similar pipeline to generate CSS variables, TypeScript constants, and design-tool exports from a single token source.

